/** Vercel serverless max duration (seconds) for cron route handlers. */
export const CRON_MAX_DURATION = 30;

/** Max notification / lifecycle emails processed per cron invocation. */
export const CRON_EMAIL_BATCH_LIMIT = 20;
