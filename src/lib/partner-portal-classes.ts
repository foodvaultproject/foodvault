/**
 * Partner Portal — SaaS admin UI tokens.
 *
 * Type scale: 22 / 18 / 16 / 14 / 13 / 12 px
 */

/** Page title (H1) — 22px, weight 700 */
export const portalPageTitle = "portal-page-title";

/** Section headings (H2) — 18px, weight 650–700 */
export const portalSectionTitle = "portal-section-title";

/** Card / block titles (H3) — 16px, weight 600 */
export const portalCardTitle = "portal-card-title";

/** Page subtitle — 13px muted */
export const portalPageSubtitle =
  "mt-1 text-[0.8125rem] leading-snug text-muted-foreground";

/** Body copy — 14px */
export const portalBody = "text-sm leading-normal text-foreground";

/** Field labels — 13px medium */
export const portalLabel = "text-[0.8125rem] font-medium text-foreground";

/** Helper / description — 12px muted */
export const portalHelper = "text-xs leading-snug text-muted-foreground";

/** Prominent data values (metrics, codes) — not headings */
export const portalMetricValue =
  "text-lg font-semibold tabular-nums text-foreground";

/** Main content shell */
export const portalPage = "mx-auto max-w-7xl px-4 pb-6 pt-2 sm:px-6 lg:px-8";

/** Narrow pages (Account, Support) */
export const portalPageNarrow = "mx-auto max-w-3xl px-4 pb-6 pt-2 sm:px-6 lg:px-8";

/** Vertical gap between sections */
export const portalSectionStack = "space-y-5";

/** Section card — compact padding */
export const portalCard =
  "rounded-lg border border-border bg-background px-5 py-4 shadow-sm";

/** Gap from card/section heading → content */
export const portalCardContent = "mt-3";

/** Standard text input — 44px height, 14px text */
export const portalInput =
  "h-11 w-full rounded-md border border-border bg-background px-3 text-sm text-foreground transition-[border-color,box-shadow] duration-150 placeholder:text-muted-foreground focus:border-primary focus:outline-none focus:ring-2 focus:ring-primary/20";

/** Select — matches input */
export const portalSelect =
  "h-11 w-full rounded-md border border-border bg-background px-3 text-sm text-foreground focus:border-primary focus:outline-none focus:ring-2 focus:ring-primary/20";

/** Textarea */
export const portalTextarea =
  "min-h-[5rem] w-full resize-y rounded-md border border-border bg-background px-3 py-2 text-sm leading-normal text-foreground placeholder:text-muted-foreground focus:border-primary focus:outline-none focus:ring-2 focus:ring-primary/20";

export const portalInputDisabled =
  "cursor-not-allowed bg-surface text-muted-foreground";

/** Gap from label → control */
export const portalFieldGap = "mt-1";

/** Inline form grid */
export const portalFormGrid = "grid gap-3 sm:grid-cols-2";

/** Outline button — 14px, 44px height */
export const portalBtnOutline =
  "inline-flex h-11 items-center justify-center rounded-sm border border-border bg-background px-4 text-sm font-medium text-foreground transition-colors hover:bg-surface";

/** Primary action — 14px */
export const portalBtnPrimary =
  "fv-btn-primary inline-flex h-11 items-center justify-center rounded-sm px-5 text-sm font-medium text-primary-foreground transition-[transform,box-shadow] duration-150 disabled:cursor-not-allowed disabled:opacity-60";

/** Ghost / text button */
export const portalBtnGhost =
  "inline-flex h-11 items-center justify-center rounded-sm px-4 text-sm font-medium text-muted-foreground transition-colors hover:text-foreground";

/** Destructive text link */
export const portalDestructive =
  "text-sm font-medium text-red-600 transition-colors hover:text-red-700";

/** Image / inline text action — 12px, subdued */
export const portalTextAction =
  "text-xs font-medium text-primary transition-colors hover:text-primary-hover";

/** Tab bar button — 14px */
export const portalTab =
  "shrink-0 rounded-t-lg px-3.5 py-2 text-sm font-medium transition-colors";

/** Table / list container */
export const portalTableWrap =
  "overflow-x-auto rounded-lg border border-border bg-background shadow-sm";

/** Image thumbnail sizes */
export const portalThumbBanner = "h-20 w-[240px] shrink-0";
export const portalThumbLogo = "h-20 w-20 shrink-0";
export const portalThumbGallery = "h-[120px] w-[96px] shrink-0";

/** Page header block spacing */
export const portalPageHeader = "mb-5";
